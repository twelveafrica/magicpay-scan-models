# Models 1.0.0

First release. The same weights the measurements in `magicpay-scan/doc` and the
corpus gates of `payment_parser` were taken on.

Sizes and sha256 of every asset are in the pointer release
(`manifest-1` → `manifest.json`). They must match the assets here; the
publishing script computes both from the same files in one pass.
